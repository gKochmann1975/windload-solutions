# robots.txt for windload.solutions

User-agent: *
Allow: /

# Sitemap location
Sitemap: https://windload.solutions/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1

# Build-only template fragments (no <head>, not real pages) — keep out of the index
Disallow: /template_header.html
Disallow: /seo-footer-template.html
